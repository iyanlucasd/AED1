#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <ctype.h>
#include <locale.h>
#include <stdbool.h>

    void EXERCICIO1()
    {
        int x;
        int y;
        printf("digite x\n");
        scanf("%i", &x);
        printf("digite y\n");
        scanf("%i", &y);
        if (x = y + 1)
        {
            printf("sim");
        }
        
}
